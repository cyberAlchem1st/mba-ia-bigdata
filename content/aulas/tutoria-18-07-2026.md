# Tutoria 18/07/2026 — Resolvendo exercício de NumPy: filtro por média e mínimo

> **Resumo didático** — tutoria do curso 1 focada em resolver, passo a passo, o exercício 18 de fixação (NumPy): comparar notas da empresa A com a média da empresa B, filtrar e calcular o mínimo ponderado. Também discutiu a diferença entre `min` do NumPy e `min` de lista, e a importância de quebrar problemas em etapas.

## O que foi coberto (cronológico)

A tutoria abriu com o William avisando que a quinzena 2 (banco de dados) foi liberada, mas que naquele momento ainda se falaria de Python (quinzena 1); na quarta-feira seguinte o tutor Afonso começaria a parte de banco de dados. O Gustavo continuaria acompanhando o fórum para dúvidas de Python.

Gustavo lembrou que preparou um **material em texto com as soluções do exercício da tutoria anterior** (list comprehension), organizado passo a passo com as três soluções diferentes.

**Exercício 18 resolvido ao vivo** (dúvida do Marcos):
- Contexto: dois arrays NumPy com notas de 1 a 10 para 4 serviços de duas empresas concorrentes, A (10 notas) e B (5 notas), organizadas em linhas; há uma lista de pesos. O exercício pede: (1) média das notas de cada serviço (usando redução); (2) para cada serviço, a **menor nota de A considerando apenas as notas maiores que a média de B** daquele serviço; (3) armazenar essas notas mínimas num novo array de 4 elementos; (4) soma das notas mínimas ponderadas pelos pesos (multiplicação vetorial).
- Dificuldade do Marcos: comparar o array de A com a média de B dava erro de tamanho de arrays (broadcast não acontece automaticamente quando os tamanhos não batem).
- Estratégia mostrada:
  1. Calcular a média de B por linha (ex.: com list comprehension `[np.mean(row) for row in B]`).
  2. Usar `enumerate` para iterar por linha **e coluna** de A, comparando cada valor com a média de B daquela linha (índice).
  3. Para valores que não passam no filtro, usar um "valor inválido" — como as notas vão de 1 a 10, usar **11** (nota inválida) em vez de 0, para não atrapalhar o cálculo do mínimo.
  4. Aplicar `np.min(..., axis=1)` para obter a menor nota de cada linha.
  5. Multiplicar vetorialmente pelos pesos e somar.
- Lição central: **quebrar o problema em etapas menores** — primeiro resolver cada parte, depois otimizar (ex.: juntar tudo num list comprehension). Não é preciso começar pela solução mais elegante.

**`np.min` vs `min` de lista** (dúvida de outro aluno):
- Para esse exercício, a diferença é irrelevante (poucas comparações). Em geral, `np.min` tende a ser mais rápido porque o NumPy é implementado em C. A forma de decidir é consultar a documentação e comparar a complexidade (Big O) de cada função.

**Discussão sobre bibliotecas e aprendizado** (Jonas, vindo de Cobol):
- Jonas comentou a diferença entre linguagens mais antigas (onde um livro cobria ~70% do que se usava) e o ecossistema atual de bibliotecas. Gustavo respondeu que não é preciso decorar bibliotecas: elas evoluem, e refazer algo que uma biblioteca já faz também é aprendizado válido.

**Encerramento**: William e Gustavo reforçaram que a tutoria é um espaço para os alunos — dúvidas, curiosidades e questionamentos sobre conteúdo são bem-vindos, independentemente de onde cada um está no curso. Dúvidas que surgirem depois podem ir para o fórum.

## Pontos-chave / Decisões ou orientações

- Quinzena 2 (banco de dados) liberada; próxima tutoria de banco com o tutor Afonso na quarta-feira.
- Material em texto com as soluções do exercício de list comprehension foi disponibilizado.
- Exercício 18 (NumPy): para filtrar notas de A maiores que a média de B, iterar por linha e coluna com `enumerate` e comparar com a média da linha correspondente.
- Usar 11 (nota inválida) como valor de "descarte" para não interferir no `np.min`.
- `np.min(..., axis=1)` calcula o mínimo por linha.
- Quebrar o problema em etapas menores antes de otimizar o código.
- `np.min` tende a ser mais rápido que `min` de lista (NumPy é em C); para o exercício, tanto faz.
- Não é preciso decorar bibliotecas; elas evoluem e refazer algo já existente também ensina.
- Tutoria é espaço ativo: trazer dúvidas e curiosidades, mesmo que esteja no começo do conteúdo.

## Ações para o aluno

- Refazer o exercício 18 de fixação seguindo as etapas mostradas (média de B → filtro com `enumerate` → valor 11 → `np.min(axis=1)` → pesos).
- Consultar a documentação do NumPy para `np.min`, `np.mean` e `enumerate`.
- Revisar o material em texto com as soluções do exercício da tutoria anterior.
- Quem não chegou ao exercício 18: continuar os exercícios de fixação no próprio ritmo e levar dúvidas às próximas tutorias.

## Materiais mencionados

- Exercício 18 de fixação (NumPy: arrays, médias, mínimo, pesos)
- Material em texto com soluções do exercício de list comprehension (disponibilizado pelo tutor)
- Documentação do NumPy (`np.min`, `np.mean`, `enumerate`)
