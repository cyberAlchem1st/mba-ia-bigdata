# Tutoria 22/07/2026 — Python com o professor Leandro: importando CSV, Colab vs VS Code e regras do curso

> **Resumo didático** — tutoria do curso 1 com a presença do professor Leandro (parte de Python). Foram mostrados, passo a passo, como importar arquivos CSV no Colab e no VS Code (instalação de kernel, Pandas e ambiente virtual), e esclarecidas regras administrativas do MBA (presença, atividades quinzenais, avaliação final e prazos).

## O que foi coberto (cronológico)

O William abriu avisando que, por causa da presença do professor Leandro, a tutoria de hoje seria de Python, e a parte de banco de dados foi deslocada para quinta-feira (23/07) às 19h, com o tutor Afonso, o William e o Thiago (apoio técnico). O objetivo da sessão: tirar dúvidas de toda a parte de Python.

**Importando arquivo CSV no Google Colab** (dúvida da Sheila, que não achava os arquivos):

- No Colab, o arquivo é enviado pela "pastinha" (ícone de arquivos) ou arrastando para o ambiente. Há uma pasta `sample_data` com dados de exemplo.
- CSV = *Comma Separated Values*: primeira linha tem os nomes das colunas; cada linha seguinte é uma instância de dado.
- Para ler CSV, normalmente se usa a biblioteca **Pandas** (`import pandas as pd`).
- Os arquivos de dados dos exercícios estão no final da quinzena 1, num **`.zip` no Drive** ("arquivos de dados para os exercícios") — a Sheila encontrou e baixou.
- Caminho do arquivo: no Colab é preciso subir o arquivo para o ambiente; no VS Code o arquivo já está no computador, basta apontar para a pasta certa (opção "copiar caminho").

**Configurando o VS Code do zero** (passo a passo mostrado pelo Gustavo):

- Criar um arquivo `.ipynb` (formato de notebook) ou `.py` (Python).
- O VS Code pede para escolher um **kernel** (interpretador Python com as bibliotecas). Criar um novo ambiente (ex.: Python 3.9).
- `import pandas` dá erro até instalar a biblioteca com o gerenciador **Pip**: `pip install pandas` (que traz dependências como NumPy e tzdata).
- Depois de instalado no kernel, os notebooks seguintes reutilizam as bibliotecas (ver com `pip list`).
- **Ambiente virtual (`venv`)**: isola bibliotecas por projeto, evitando conflitos de versões entre projetos. Para ativar no Windows: `venv\Scripts\activate` (sem `source`, que é do Linux).
- Caso do Carlos (computador da empresa): o VS Code não achava o kernel/Python; a solução passava por criar/ativar o `venv` e instalar o Pip — mas a política de segurança da empresa bloqueava a instalação, então ele precisaria pedir liberação à TI.

**Regras administrativas do MBA** (pergunta do Lucas):

- **Presença**: é computada fazendo a atividade quinzenal.
- **Nota**: peso das atividades quinzenais + avaliação final.
- **Prazos**: por causa dos períodos de acesso, as atividades do curso 0 e do curso 1 foram prorrogadas — todas terminam em **24 de agosto** (mesma data da avaliação final). Normalmente cada atividade quinzenal fica aberta por duas semanas.
- A avaliação final cobre o conteúdo das quinzenas 1 e 2.
- Dúvidas sobre notas/presença: o William se comprometeu a enviar as instruções de onde ver isso no perfil do Moodle.

**Encerramento**: lembrado que amanhã (23/07) às 19h haverá a primeira tutoria de banco de dados, com foco em configuração e conexão (dúvidas de CPF/conexão já vistas no fórum). Sheila perguntou sobre o link do Zoom — a agenda foi atualizada com o novo horário.

## Pontos-chave / Decisões ou orientações

- Importar CSV no Colab: enviar o arquivo pelo painel de arquivos e ler com Pandas; arquivos dos exercícios vêm num `.zip` no Drive.
- VS Code: precisa de kernel Python + bibliotecas instaladas via Pip; ambiente virtual (`venv`) isola bibliotecas por projeto.
- Colab é mais simples para começar (tudo pronto); VS Code é melhor para dados grandes/sensíveis (privacidade).
- Presença = fazer a atividade quinzenal; nota = atividades quinzenais + avaliação final.
- Prazos do curso 0 e curso 1 prorrogados: tudo termina em **24/08**.
- Tutoria de banco de dados: quinta-feira 23/07 às 19h (Afonso + William + Thiago), foco em configuração e conexão.
- Dúvidas não resolvidas na tutoria são respondidas nos canais de comunicação (fórum).

## Ações para o aluno

- Baixar o `.zip` "arquivos de dados para os exercícios" no final da quinzena 1 e praticar a importação de CSV no Colab.
- Quem usa VS Code: configurar kernel, instalar Pandas via Pip e criar ambiente virtual (`venv`); quem tem bloqueio da empresa, pedir liberação à TI.
- Fazer a atividade quinzenal para garantir presença e nota.
- Acompanhar os prazos (24/08) e a agenda (tutoria de banco em 23/07 às 19h).
- Postar dúvidas no fórum do curso 1.

## Materiais mencionados

- Google Colab (painel de arquivos, `sample_data`)
- VS Code (kernel, extensões Python/Jupyter, terminal, `venv`)
- Pandas (`import pandas as pd`) e Pip (`pip install pandas`, `pip list`)
- Arquivo `.zip` "arquivos de dados para os exercícios" (Drive, final da quinzena 1)
- Moodle (atividades quinzenais, avaliação final, agenda)
